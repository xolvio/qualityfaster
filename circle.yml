machine:
  java:
    version: oraclejdk8
  node:
    version: 6.5.0

dependencies:
  cache_directories:
    - "node_modules"
    - "~/.npm"

  override:
    # Cache npm deps
    - npm install --loglevel=error

test:
  override:
    - npm test
